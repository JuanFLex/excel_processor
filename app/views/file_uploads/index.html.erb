<div class="container mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold mb-6">Procesador de Excel</h1>
  
  <div class="mb-6">
    <%= link_to "Subir nuevo archivo", new_file_upload_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
    <%= link_to "Gestionar referencias de commodities", commodity_references_path, class: "bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded ml-2" %>
  </div>
  
  <h2 class="text-xl font-semibold mb-4">Archivos procesados recientemente</h2>
  
  <div class="bg-white shadow-md rounded-lg overflow-hidden">
    <table class="min-w-full">
      <thead>
        <tr class="bg-gray-100 border-b">
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Archivo</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Estado</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Fecha</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Acciones</th>
        </tr>
      </thead>
      <tbody>
        <% @processed_files.each do |file| %>
          <tr class="border-b hover:bg-gray-50">
            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
              <%= file.original_filename %>
            </td>
            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
              <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full 
                <%= file.completed? ? 'bg-green-100 text-green-800' : 
                    file.failed? ? 'bg-red-100 text-red-800' : 
                    file.processing? ? 'bg-yellow-100 text-yellow-800' : 'bg-gray-100 text-gray-800' %>">
                <%= file.status.titleize %>
              </span>
            </td>
            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
              <%= file.created_at.strftime("%d/%m/%Y %H:%M") %>
            </td>
            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
              <%= link_to "Ver detalles", file_upload_path(file), class: "text-indigo-600 hover:text-indigo-900 mr-3" %>
              <% if file.completed? && file.result_file_path.present? %>
                <%= link_to "Descargar", download_file_upload_path(file), class: "text-green-600 hover:text-green-900" %>
              <% end %>
            </td>
          </tr>
        <% end %>
        
        <% if @processed_files.empty? %>
          <tr>
            <td colspan="4" class="px-6 py-4 text-center text-gray-500">
              No hay archivos procesados aún.
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>