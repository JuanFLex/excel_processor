<!DOCTYPE html>
<html>
  <head>
    <title>Excel Processor</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application.tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 min-h-screen">
    <header style="background-color: #5d6268; color: white;">
      <div class="container mx-auto px-4 py-0">
        <div class="flex justify-between items-center">
          <h1 class="text-xl font-bold">
            <%= link_to image_tag("logo.png", alt: "Logo", class: "h-12 inline"), root_path %>
          </h1>
          <nav>
            <ul class="flex space-x-4">
              <li><%= link_to "Home", root_path, class: "hover:underline" %></li>
              <li><%= link_to "Upload file", new_file_upload_path, class: "hover:underline" %></li>
              <li><%= link_to "References", commodity_references_path, class: "hover:underline" %></li>
            </ul>
          </nav>
        </div>
      </div>
    </header>

    <main>
      <% if notice %>
        <div class="container mx-auto px-4 mt-4">
          <div class="flash-notice bg-orange-600 text-white p-2 rounded">
            <%= notice %>
          </div>
        </div>
      <% end %>
      
      <% if alert %>
        <div class="container mx-auto px-4 mt-4">
          <div class="flash-alert bg-red-500 text-white p-2 rounded">
            <%= alert %>
          </div>
        </div>
      <% end %>
      
      <%= yield %>
    </main>

    <footer style="background-color: #101920; color: white; margin-top: 2rem;">
      <div class="container mx-auto px-4 py-6">
        <p class="text-center text-gray-400 text-sm">
          Excel Processor &copy; <%= Date.current.year %>
        </p>
      </div>
    </footer>
  </body>
</html>
