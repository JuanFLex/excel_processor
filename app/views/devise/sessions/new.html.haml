-# This file uses the application layout, so we only need the content within the main section

.container.mx-auto.px-4.py-12
  .flex.justify-center.items-center.min-h-64
    .w-full.max-w-md
      .bg-white.rounded-2xl.shadow-lg.p-10
        .text-center.mb-8
          %h2.text-3xl.font-bold.text-gray-900
            Welcome Back
          %p.mt-2.text-gray-600
            Sign in to your account to continue

        = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
          .space-y-6
            .field
              = f.label :email, class: "block text-sm.font-medium.text-gray-700.mb-1"
              = f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full.px-4.py-3.border.border-gray-300.rounded-lg.focus:ring-2.focus:ring-primary-600.focus:border-primary-500.transition.duration-200.placeholder-gray-400"

            .field
              = f.label :password, class: "block text-sm.font-medium.text-gray-700.mb-1"
              = f.password_field :password, autocomplete: "current-password", class: "w-full.px-4.py-3.border.border-gray-300.rounded-lg.focus:ring-2.focus:ring-primary-600.focus:border-primary-500.transition.duration-200.placeholder-gray-400"

            .flex.items-center.justify-between
              - if devise_mapping.rememberable?
                .flex.items-center
                  = f.check_box :remember_me, class: "h-4.w-4.text-primary-600.focus:ring-primary-500.border-gray-300.rounded"
                  = f.label :remember_me, class: "ml-2.block.text-sm.text-gray-700"
              
              = link_to "Forgot password?", new_password_path(resource_name), class: "text-sm.font-medium.text-primary-600.hover:text-primary-700.transition.duration-200"

            .mt-6
              = f.submit "Sign in", class: "w-full.py-3.px-4.bg-primary-600.hover:bg-primary-700.text-white.font-medium.rounded-lg.transition.duration-200.ease-in-out.focus:outline-none.focus:ring-2.focus:ring-offset-2.focus:ring-primary-500"

        .mt-8
          .relative
            .absolute.inset-0.flex.items-center
              .w-full.border-t.border-gray-200
            .relative.flex.justify-center
              %span.bg-white.px-4.text-sm.text-gray-500
                Or
          .mt-6.text-center
            %p.text-sm.text-gray-600
              Don't have an account?
            = link_to "Create account", new_registration_path(resource_name), class: "mt-1 text-sm font-medium text-primary-600 hover:text-primary-700 inline-block transition duration-200"

        .mt-8.text-center
          = link_to "← Back to homepage", root_path, class: "text-sm text-gray-500 hover:text-gray-700 transition duration-200" 