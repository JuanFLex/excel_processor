-# This file uses the application layout, so we only need the content within the main section

.container.mx-auto.px-4.py-4
  .flex.justify-center.items-center.min-h-80
    .w-full.max-w-md
      .bg-white.rounded-2xl.shadow-lg.p-6
        .text-center.mb-4
          -# Logo
          .mb-1
            = image_tag "SmartQuoteLogo.png", alt: "Smart Quote Logo", class: "mx-auto h-10 w-auto object-cover object-center", style: "clip-path: inset(10% 0 30% 0);"
          %h2.text-xl.font-bold.text-gray-900
            Welcome Back
          %p.mt-1.text-sm.text-gray-600
            Sign in to continue

        = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
          .space-y-3
            .field
              = f.label :email, class: "block text-sm font-medium text-gray-700 mb-1"
              = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Enter your email", class: "w-full px-3 py-2 bg-gray-50 border-2 border-gray-200 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 focus:bg-white transition duration-200 text-gray-900 placeholder-gray-500"

            .field
              = f.label :password, class: "block text-sm font-medium text-gray-700 mb-1"
              = f.password_field :password, autocomplete: "current-password", placeholder: "Enter your password", class: "w-full px-3 py-2 bg-gray-50 border-2 border-gray-200 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 focus:bg-white transition duration-200 text-gray-900 placeholder-gray-500"

            .flex.items-center.justify-between
              - if devise_mapping.rememberable?
                .flex.items-center
                  = f.check_box :remember_me, class: "h-4.w-4.text-primary-600.focus:ring-primary-500.border-gray-300.rounded"
                  = f.label :remember_me, class: "ml-2.block.text-sm.text-gray-700"

              = link_to "Forgot password?", new_password_path(resource_name), class: "text-sm font-medium text-blue-600 hover:text-blue-700 transition duration-200"

            .mt-4
              = f.submit "Sign in", class: "w-full py-2 px-4 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-lg transition duration-200 ease-in-out focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 shadow-sm hover:shadow-md"

        .mt-4
          .relative
            .absolute.inset-0.flex.items-center
              .w-full.border-t.border-gray-200
            .relative.flex.justify-center
              %span.bg-white.px-3.text-sm.text-gray-500
                Or
          .mt-4.text-center
            %p.text-sm.text-gray-600
              Don't have an account?
            = link_to "Create account", new_registration_path(resource_name), class: "text-sm font-medium text-blue-600 hover:text-blue-700 inline-block transition duration-200"

        .mt-4.text-center
          = link_to "← Back to homepage", root_path, class: "text-sm text-gray-500 hover:text-gray-700 transition duration-200" 